name: Deploy Pages

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Download Godot
        run: |
          set -euo pipefail
          curl -L -o godot.zip https://github.com/godotengine/godot-builds/releases/download/4.7.1-stable/Godot_v4.7.1-stable_linux.x86_64.zip
          unzip -o godot.zip
          mkdir -p "$HOME/.local/bin"
          install -m 0755 Godot_v4.7.1-stable_linux.x86_64 "$HOME/.local/bin/godot"
          echo "$HOME/.local/bin" >> "$GITHUB_PATH"

      - name: Download export templates
        run: |
          set -euo pipefail
          curl -L -o templates.tpz https://github.com/godotengine/godot-builds/releases/download/4.7.1-stable/Godot_v4.7.1-stable_export_templates.tpz
          mkdir -p "$HOME/.local/share/godot/export_templates/4.7.1.stable"
          unzip -o templates.tpz
          cp -r templates/. "$HOME/.local/share/godot/export_templates/4.7.1.stable/"

      - name: Import and export Godot Web
        run: |
          set -euo pipefail
          mkdir -p web
          godot --headless --path godot --import
          godot --headless --path godot --export-release Web ../web/index.html
          cp tools/gzip_fetch.js web/gzip_fetch.js
          touch web/.nojekyll
          gzip -9 -c web/index.wasm > web/index.wasm.gz
          gzip -9 -c web/index.pck > web/index.pck.gz
          rm -f web/index.wasm web/index.pck

      - name: Upload Pages artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: web

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
